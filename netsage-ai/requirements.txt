# NetSage AI — Requirements
# Base install (no provider SDK required for Ollama or OpenRouter)
streamlit>=1.32.0
pandas>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0
pytest>=8.0.0

# --- Optional Provider SDKs ---
# Install ONLY the SDK for the provider you select via AI_PROVIDER env var.
#
# AI_PROVIDER=anthropic  → pip install anthropic>=0.25.0
# AI_PROVIDER=gemini     → pip install google-generativeai>=0.5.0
#
# AI_PROVIDER=ollama     → no extra package needed (uses requests)
# AI_PROVIDER=openrouter → no extra package needed (uses requests)
